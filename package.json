{
	"name": "zkali-agents",
	"version": "0.1.0",
	"description": "DeepSeek multi-agent contract and runtime types",
	"type": "module",
	"main": "./dist/types/index.js",
	"types": "./dist/types/index.d.ts",
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {
		"axios": "^1.9.0",
		"better-sqlite3": "^12.9.0",
		"dotenv": "^16.4.5"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.13",
		"typescript": "^5.8.3",
		"vitest": "^4.1.5"
	}
}
